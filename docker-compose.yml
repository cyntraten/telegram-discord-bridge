services:
  bridge:
    build: .
    container_name: tg-discord-bridge
    restart: unless-stopped
#    network_mode: "host" <- Это локально затестить со стариной винни потому что РКН блочит телегу и дискорд
    env_file:
      - .env
